Map<String, String> imports() {
    [
            commons            : 'jenkins/src/main/lib/commons.jenkins'
    ]
}

def execute() {
        node() {
            stage('checkout') {
                git 'https://github.com/lesfurets/randori.git'
            }
            stage('ls') {
                sh 'git branch -a'
            }
        }
}

return this
